"use client";
import PageBuilder from "@/components/PageBuilder";
import { aboutData } from "@/library/data/about";

const Page = () => {
  return <PageBuilder data={aboutData} />;
};

export default Page;
